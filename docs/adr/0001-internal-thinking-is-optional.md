# Internal Thinking is an Optional Capability

The streaming contract requires safe user-visible Thinking Processes as the base progress signal, while raw Internal Thinking is an optional capability for Remote Agent implementations. The Reference Implementation favors full visibility and shows Internal Thinking when the implementing Remote Agent supports it, without adding a separate configuration gate. Other remotes own their own visibility, safety, privacy, storage, and UI policies.
