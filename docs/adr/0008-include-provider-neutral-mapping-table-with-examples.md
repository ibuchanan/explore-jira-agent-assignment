# Include a Provider-Neutral Mapping Table with Examples

The streaming spec includes a provider-neutral mapping table that names Remote Agent Signal categories, gives concrete coding-agent scenarios, and maps each category to A2A wire events, state or content behavior, and finality. The examples follow a Jira work item that requires repository changes, including interruption and resumption, making the categories implementable without baking provider-specific runtime event names into the reference integration.
