# Final Summary Uses Status and Artifact

Final completion uses both the task status and artifact channels when summary details are needed. The `completed` status message remains brief and lifecycle-oriented, while the reviewable implementation summary is emitted as an A2A Artifact. This keeps completion semantics clear and preserves task outputs in Jira's artifact channel.
