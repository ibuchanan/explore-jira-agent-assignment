# Input-Required Is for Work Clarification

The Reference Implementation uses `input-required` when the Remote Agent needs a user decision or clarification about the work, such as repository, branch, acceptance criteria, or an ambiguous requirement. It uses `auth-required` when the user needs to grant permission or approve a privileged capability or tool action.
