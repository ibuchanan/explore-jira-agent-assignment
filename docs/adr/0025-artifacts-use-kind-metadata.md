# Artifacts Can Use Kind Metadata

Coding-agent Artifacts can include a conventional kind value in artifact metadata, such as `patch`, `pull-request`, `test-report`, or `implementation-summary`, when that helps clients or tests understand the artifact. Artifact `name` and `description` remain display text. The Reference Implementation does not define a required metadata key schema because that is more detail than this sample app needs.
