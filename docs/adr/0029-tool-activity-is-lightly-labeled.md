# Tool Activity Is Lightly Labeled

The Reference Implementation labels Tool Activity messages in text, with labels such as `Tool:` for invocations and `Tool result:` for results treated as illustrative rather than required protocol strings. This makes the runtime/tool flow visible to implementers without defining a custom message schema.
