# Input Needs Are Clearly Labeled

The Reference Implementation labels Input Need messages clearly in text, with labels such as `Input required:` treated as illustrative rather than required protocol strings. Input Needs are paired with `input-required` and ask the user to clarify the work, so they should be explicit without requiring a custom message schema.
